services:
  pythonapp:
    build: /home/nacho/Facultad/Aninfo/repo_sq_8/memoSq8
    command: uvicorn main:app
    depends_on:
      - mysql
    ports:
      - 8008:8000
    links:
      - mysql
  
  mysql:
    image: mysql
    restart: always
    environment:
      MYSQL_DATABASE: flutter
      MYSQL_USER: admin
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: password
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: